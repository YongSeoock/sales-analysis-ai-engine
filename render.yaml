services:
  - type: web
    name: sales-analysis-ai-engine
    runtime: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GCP_API_KEY
        sync: false
